import { PciChargerToTextPipe } from './pci-charger-to-text.pipe';

describe('PciChargerToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new PciChargerToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
